//! Backward-compatible module path — implementation lives in `translation_normalize.rs`.

#[allow(unused_imports)]
pub use crate::translation_normalize::{
    default_translation_provider_settings, normalize_translation_provider_settings,
};
