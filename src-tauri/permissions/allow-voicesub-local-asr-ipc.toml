[[permission]]
identifier = "allow-voicesub-local-asr-ipc"
description = "Local ASR module webview IPC — loopback token and allowlisted external URLs only."
commands.allow = [
  "get_loopback_api_token",
  "open_external_https_url",
]
