Bundled reference data for VoiceSub (migrated from the removed SST Python backend).

- config.example.json — sample user config
- config.schema.json — JSON schema reference
- source_text_builtin_pairs.json — built-in source-text replacement pairs
- dictionary_overrides.example.json — sample dictionary overrides

Runtime config lives in user-data/config.toml (Rust voicesub-config).
