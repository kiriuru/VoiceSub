# Legacy (VoiceSub)

Archived SST 0.4.x surfaces that are **not** part of the VoiceSub 0.5 core stack.

| Path | Contents |
|------|----------|
| `remote/` | Remote controller/worker HTML/JS bridges (frozen; not wired in Rust runtime) |
| `experimental-browser/` | Experimental browser ASR page |

Active development: **Rust + Tauri + Svelte** (`start-voicesub.bat`, `crates/`, `src/`).

SST 0.4.4 Python reference line: `F:\AI\stream-sub-translator`.
